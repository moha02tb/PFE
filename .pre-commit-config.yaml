# Minimal pre-commit configuration
# Due to Python 3.12 compatibility issues with isolated environments,
# consider running linting tools separately (ruff, prettier, eslint)
# rather than through pre-commit's isolated venv approach.

repos: []

default_language_version:
  python: python3